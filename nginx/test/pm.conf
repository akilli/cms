server {
    listen 443 ssl http2;
    server_name pm.test.eqmh.de;
    root /app/pm;
    include snippets/vhost.conf;

    location ~* ^/(app|data|plugins|vendor|config.php|kanboard) {
        deny all;
        return 404;
    }
}
