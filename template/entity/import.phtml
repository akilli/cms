<?php namespace qnd; /** @var callable $§ */?>
<form id="<?=$§('id');?>" action="<?= url('*/import');?>" method="post" enctype="multipart/form-data">
    <h1><?= _('Import');?></h1>
    <?php if ($§('entity') === 'page'):?>
        <?php $accept = '.html, .odt';?>
    <p><?= _('Import one or multiple pages as HTML or ODT files');?></p>
    <?php elseif ($§('entity') === 'project'):?>
        <?php $accept = '.zip';?>
    <p><?= _('Import one or multiple projects as ZIP files');?></p>
    <?php else:?>
        <?php $accept = file_accept('file');?>
    <p><?= _('Import one or multiple media files');?></p>
    <?php endif;?>
    <input type="file" name="data[import][]" required="required" multiple="multiple" accept="<?= $accept;?>" />
    <input type="submit" value="file_upload" class="icon" title="<?= _('Upload');?>" />
    <input type="hidden" name="token" value="<?= token();?>" />
</form>
