<?php namespace qnd; /** @var callable $§ */?>
<section id="<?=$§('§')['id'];?>" class="<?= $§('context');?>">
    <h1><?=$§('title');?></h1>
    <form method="post" enctype="multipart/form-data">
        <?php foreach ($§('data') as $id => $item):?>
        <fieldset>
            <legend><?=$item['name'] ?? _('New Entry');?></legend>
            <?php foreach ($item['_entity']['attr'] as $attr):?>
                <?php $attr['context'] = $§('context');?>
                <?php if (!$html = editor($attr, $item)) continue;?>
            <div data-attr="<?=$attr['uid'];?>">
                <?=$html;?>
            </div>
            <?php endforeach;?>
        </fieldset>
        <?php endforeach;?>
        <?php if (allowed('admin')):?>
        <a role="button" href="<?= url('*/admin');?>"><?= _('Back');?></a>
        <?php endif;?>
        <input type="submit" value="<?= _('Save');?>" />
        <input type="hidden" name="token" value="<?= token();?>" />
    </form>
</section>
