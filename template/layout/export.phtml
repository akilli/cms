<?php namespace qnd; /** @var callable $§ */?>
<!doctype html>
<html>
    <head>
        <meta charset="<?= data('app', 'charset');?>" />
        <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1" />
        <title><?=$§('title');?></title>
        <link rel="stylesheet" href="<?=url_theme('theme.css');?>" />
    </head>
    <body>
        <header id="header">
            <h1><a href="<?= url();?>"><?= $§('name');?></a></h1>
            <div class="logo">
                <img src="<?= project('image');?>" alt="<?= _('Logo');?>" />
            </div>
        </header>
        <?= $§('nav');?>
        <main>
            <?= $§('main');?>
            <?php if ($§('subnav')):?>
            <aside id="sidebar">
                <?= $§('subnav');?>
            </aside>
            <?php endif;?>
        </main>
    </body>
</html>
