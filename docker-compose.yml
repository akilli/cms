version: "3.5"
networks:
  default:
    external:
      name: proxy
volumes:
  app: {}
  asset: {}
  db: {}
services:
  cms:
    image: akilli/nginx
    container_name: cms
    depends_on:
      - cms-app
      - cms-data
    restart: unless-stopped
    labels:
      traefik.enable: "true"
      traefik.port: "80"
      traefik.frontend.rule: "Host: cms.app.loc"
    volumes:
      - source: app
        target: /app
        type: volume
        volume:
          nocopy: true
      - source: asset
        target: /data/asset
        type: volume
        volume:
          nocopy: true
  cms-app:
    image: akilli/cms
    container_name: cms-app
    depends_on:
      - cms-data
    restart: unless-stopped
    volumes:
      - source: app
        target: /app
        type: volume
      - source: asset
        target: /data/asset
        type: volume
        volume:
          nocopy: true
  cms-data:
    image: akilli/cms-data
    container_name: cms-data
    restart: unless-stopped
    volumes:
      - source: asset
        target: /app
        type: volume
      - source: db
        target: /data
        type: volume
