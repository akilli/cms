<?php
declare(strict_types=1);
namespace app;
/** @var callable $var */
?>
<nav class="pager" aria-label="pagination">
    <div class="pager-info">
        <?php echo $var('info'); ?>
    </div>
    <div class="pager-links">
    <?php foreach ($var('links') as $item): ?>
        <a
            href="<?php echo $item['url']; ?>"<?php if ($item['class']): ?>
            class="<?php echo $item['class']; ?>"<?php endif; ?><?php if ($item['current']): ?>
            aria-current="page"<?php endif; ?>
        ><?php echo $item['name']; ?></a>
    <?php endforeach; ?>
    </div>
</nav>
