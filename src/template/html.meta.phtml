<?php namespace akilli; /** @var callable $§ */?>
<meta charset="<?= config('i18n.charset');?>" />
<?php foreach (array_replace(config('meta'), $§()) as $name => $content):?>
<meta name="<?=$name;?>" content="<?=$content;?>" />
<?php endforeach;?>
