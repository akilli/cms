<?php namespace qnd; /** @var callable $§ */?>
<section class="section section-list">
    <header>
        <h1><?=$§('title');?></h1>
    </header>
<?php if ($§('data')):?>
    <?php foreach ($§('data') as $id => $item):?>
    <article>
        <?php foreach ($§('attr') as $code => $attr):?>
            <?php $attr['context'] = 'list';?>
            <?php if (!$html = viewer($attr, $item)) continue;?>
            <?php if ($code === 'name'):?>
        <header>
                <?php if (allowed('view')):?>
            <h2><a href="<?= url('*/view', ['id' => $id]);?>" title="<?= _('View this item');?>"><?=$html;?></a></h2>
                <?php else:?>
            <h2><?=$html;?></h2>
                <?php endif;?>
        </header>
            <?php else:?>
        <div class="attr-<?=$attr['id'];?>">
            <?=$html;?>
        </div>
            <?php endif;?>
        <?php endforeach;?>
    </article>
        <?php endforeach;?>
    <?= §('pager');?>
<?php else:;?>
    <p><?= _('Nothing created yet');?></p>
<?php endif;?>
</section>
