include snippets/upstream.conf;
proxy_cache_path /tmp levels=1 keys_zone=resized:1m max_size=256m;
limit_req_zone "1" zone=2persec:32k rate=2r/s;

#
# Default server
#
server {
    listen 80 default_server;
    server_name _;
    root /app/www;

    include snippets/favicon.conf;
    include snippets/redirect.conf;
    include snippets/deny.conf;
    include snippets/php.conf;
    include snippets/root.conf;

    location ~ ^/gui/(?:\d+)/(.+)$ {
        alias /app/gui/$1;
    }

    location /gui {
        alias /app/gui;
    }

    location ~ ^/ext/(?:\d+)/(.+)$ {
        alias /data/ext/gui/$1;
    }

    location /ext {
        alias /data/ext/gui;
    }

    location ~ ^/file/((?:[a-z0-9_\-]+)\.(?:[a-z0-9]+))$ {
        alias /data/file/$1;
    }

    location ~ ^/file/(?:resize|crop)-(?:\d+)(?:x\d+)?/(?:[a-z0-9_\-]+)\.(?:jpg|png|webp)$ {
        proxy_pass http://127.0.0.1:9001;
        proxy_cache resized;
        proxy_cache_use_stale error timeout invalid_header updating;
        proxy_cache_valid 180m;
    }

    include /data/ext/*.conf;
}

#
# Responsive image server
#
server {
    listen 9001;
    allow 127.0.0.1;
    deny all;
    limit_req zone=2persec burst=10;

    location ~ ^/file/resize-(\d+)/([a-z0-9_\-]+\.(?:jpg|png|webp))$ {
        alias /data/file/$2;
        image_filter_buffer 10M;
        image_filter resize $1 -;
    }

    location ~ ^/file/resize-(\d+)x(\d+)/([a-z0-9_\-]+\.(?:jpg|png|webp))$ {
        alias /data/file/$3;
        image_filter_buffer 10M;
        image_filter resize $1 $2;
    }

    location ~ ^/file/crop-(\d+)/([a-z0-9_\-]+\.(?:jpg|png|webp))$ {
        alias /data/file/$2;
        image_filter_buffer 10M;
        image_filter crop $1 -;
    }

    location ~ ^/file/crop-(\d+)x(\d+)/([a-z0-9_\-]+\.(?:jpg|png|webp))$ {
        alias /data/file/$3;
        image_filter_buffer 10M;
        image_filter crop $1 $2;
    }
}
