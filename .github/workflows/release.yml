name: release
on:
  workflow_dispatch:
  push:
    branches:
      - master
    paths-ignore:
      - '.github/**'
jobs:
  dockerhub:
    uses: akilli/docker/.github/workflows/dockerhub.yml@master
    secrets: inherit
