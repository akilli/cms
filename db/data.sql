START TRANSACTION;

-- ---------------------------------------------------------------------------------------------------------------------
-- Role
-- ---------------------------------------------------------------------------------------------------------------------

INSERT INTO
    role
    (id, name, priv)
VALUES
    (1, 'admin', '{"_all_"}');

SELECT setval('role_id_seq', (SELECT max(id) FROM role));

-- ---------------------------------------------------------------------------------------------------------------------
-- Account
-- ---------------------------------------------------------------------------------------------------------------------

INSERT INTO
    account
    (id, name, role_id, username, password)
VALUES
    (1, 'Admin', 1, 'admin', '$2y$10$FZSRqIGNKq64P3Rz27jlzuKuSZ9Rik9qHnqk5zH2Z7d67.erqaNhy');

SELECT setval('account_id_seq', (SELECT max(id) FROM account));

-- ---------------------------------------------------------------------------------------------------------------------
-- Page
-- ---------------------------------------------------------------------------------------------------------------------

INSERT INTO
    page_content
    (id, name, entity_id, slug, menu, status)
VALUES
    (1, 'Homepage', 'page_content', 'index', TRUE, 'published');

SELECT setval('page_id_seq', (SELECT max(id) FROM page));

-- ---------------------------------------------------------------------------------------------------------------------

COMMIT;
